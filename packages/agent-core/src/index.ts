// @alcode/agent-core — placeholder. The public API is authored in the
// ownership-conversion commit. The imported pi agent-loop slice lives under
// ./imported/ and is excluded from the build until converted.
export {};
