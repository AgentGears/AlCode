from pathlib import Path


def replace_once(text: str, old: str, new: str, label: str) -> str:
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"{label}: expected exactly one match, found {count}")
    return text.replace(old, new, 1)

# ---------------------------------------------------------------------------
# Program dispatch: atomically route a root operation as Program-protected or
# ordinary under the same Workspace coordinator + canonical admission lane.
# ---------------------------------------------------------------------------
path = Path("packages/host-runtime/src/program-dispatch.ts")
text = path.read_text()
text = replace_once(
    text,
    '''export interface ProgramRootOperationAuthorityV1 {\n  resolveCurrentOperation(sessionId: EventSessionId): Promise<ProgramRootOperationContextV1 | null>;\n  appendRootOperation(\n    input: ProgramRootOperationInputV1,\n    drafts: readonly EventDraft<string, unknown>[],\n  ): Promise<PersistedDomainEvent<string, unknown>[]>;\n}\n''',
    '''export interface ProgramRoutedRootOperationInputV1 {\n  sessionId: EventSessionId;\n  operationId: string;\n  workspaceAccessClass: "no_workspace_access" | "read_only" | "may_write";\n  program?: ProgramRootOperationContextV1;\n  drafts: readonly EventDraft<string, unknown>[];\n}\n\nexport type ProgramRoutedRootOperationResultV1 =\n  | {\n      status: "appended";\n      events: PersistedDomainEvent<string, unknown>[];\n      program: ProgramRootOperationContextV1 | null;\n    }\n  | { status: "program_may_write_blocked"; program: ProgramRootOperationContextV1 };\n\nexport interface ProgramRootOperationAuthorityV1 {\n  resolveCurrentOperation(sessionId: EventSessionId): Promise<ProgramRootOperationContextV1 | null>;\n  appendRoutedRootOperation(\n    input: ProgramRoutedRootOperationInputV1,\n  ): Promise<ProgramRoutedRootOperationResultV1>;\n  appendRootOperation(\n    input: ProgramRootOperationInputV1,\n    drafts: readonly EventDraft<string, unknown>[],\n  ): Promise<PersistedDomainEvent<string, unknown>[]>;\n}\n''',
    "routed root operation interface",
)
old_resolver = '''export async function resolveCurrentProgramOperationContext(\n  store: WorkspaceEventStore,\n  sessionId: EventSessionId,\n): Promise<ProgramRootOperationContextV1 | null> {\n  const events = await replayAll(store);\n  let current: ProgramRootOperationContextV1 | null = null;\n  for (const [programStateId, state] of latestProgramStates(events)) {\n    const attempt = state.lifecycle === "active" ? state.activeAttempt : null;\n    if (attempt === null || String(attempt.sessionId) !== String(sessionId)) continue;\n    const candidate: ProgramRootOperationContextV1 = {\n      programStateId,\n      expectedProgramRevision: state.revision,\n      programAttemptId: String(attempt.programAttemptId),\n      workItemId: String(attempt.workItemId),\n      agentGeneration: attempt.agentGeneration,\n    };\n    if (current !== null) {\n      throw new ProgramDispatchControlError(\n        `Multiple active ProgramAttempts claim session ${String(sessionId)}`,\n      );\n    }\n    current = candidate;\n  }\n  return current;\n}\n'''
new_resolver = '''function currentProgramOperationContextFromEvents(\n  events: readonly PersistedDomainEvent<string, unknown>[],\n  sessionId: EventSessionId,\n): ProgramRootOperationContextV1 | null {\n  let current: ProgramRootOperationContextV1 | null = null;\n  for (const [programStateId, state] of latestProgramStates(events)) {\n    const attempt = state.lifecycle === "active" ? state.activeAttempt : null;\n    if (attempt === null || String(attempt.sessionId) !== String(sessionId)) continue;\n    const candidate: ProgramRootOperationContextV1 = {\n      programStateId,\n      expectedProgramRevision: state.revision,\n      programAttemptId: String(attempt.programAttemptId),\n      workItemId: String(attempt.workItemId),\n      agentGeneration: attempt.agentGeneration,\n    };\n    if (current !== null) {\n      throw new ProgramDispatchControlError(\n        `Multiple active ProgramAttempts claim session ${String(sessionId)}`,\n      );\n    }\n    current = candidate;\n  }\n  return current;\n}\n\nexport async function resolveCurrentProgramOperationContext(\n  store: WorkspaceEventStore,\n  sessionId: EventSessionId,\n): Promise<ProgramRootOperationContextV1 | null> {\n  return currentProgramOperationContextFromEvents(await replayAll(store), sessionId);\n}\n'''
text = replace_once(text, old_resolver, new_resolver, "atomic resolver helper")
marker = '''  async appendRootOperation(\n    input: ProgramRootOperationInputV1,\n    drafts: readonly EventDraft<string, unknown>[],\n  ): Promise<PersistedDomainEvent<string, unknown>[]> {\n'''
method = '''  async appendRoutedRootOperation(\n    input: ProgramRoutedRootOperationInputV1,\n  ): Promise<ProgramRoutedRootOperationResultV1> {\n    const operationId = String(asOperationId(input.operationId));\n    const sessionId = String(input.sessionId);\n    if (input.drafts.length === 0 || input.drafts[0]?.type !== "operation.requested") {\n      throw new ProgramDispatchControlError("Root operation must begin with operation.requested");\n    }\n\n    return this.options.workspaceCoordinator.runExclusive(async () => {\n      const preliminaryEvents = await replayAll(this.options.store);\n      const preliminaryProgram = input.program ??\n        currentProgramOperationContextFromEvents(preliminaryEvents, input.sessionId);\n      let protectedObservation: ProgramAttemptExecutionBase | null = null;\n      if (preliminaryProgram !== null && preliminaryProgram !== undefined &&\n          input.workspaceAccessClass !== "may_write") {\n        const observation = await this.options.observations.observe();\n        if (observation.status === "unknown") {\n          throw new ProgramDispatchStaleError(`Current execution base is unavailable: ${observation.reason}`);\n        }\n        requireObservationWorkspace(this.options.store, observation.base);\n        protectedObservation = observation.base;\n      }\n\n      return this.options.admission.enqueue(async () => {\n        const events = await replayAll(this.options.store);\n        const canonicalProgram = currentProgramOperationContextFromEvents(events, input.sessionId);\n        const program = input.program ?? canonicalProgram;\n\n        if (program === null || program === undefined) {\n          const ordinary = input.drafts.map((draft) => {\n            if (String(draft.workspaceId) !== this.options.store.workspaceId) {\n              throw new ProgramDispatchControlError("Root operation draft belongs to another Workspace");\n            }\n            if (String(draft.sessionId) !== sessionId) {\n              throw new ProgramDispatchControlError("Root operation draft session does not match request session");\n            }\n            if (draft.operationId === undefined || String(draft.operationId) !== operationId) {\n              throw new ProgramDispatchControlError("Root operation draft operationId does not match root operation");\n            }\n            if (draft.programStateId !== undefined) {\n              throw new ProgramDispatchControlError("Ordinary root operation may not carry ProgramStateId");\n            }\n            return draft;\n          });\n          return {\n            status: "appended",\n            events: await this.options.store.append(ordinary),\n            program: null,\n          } as const;\n        }\n\n        if (input.workspaceAccessClass === "may_write") {\n          return { status: "program_may_write_blocked", program } as const;\n        }\n        if (protectedObservation === null) {\n          throw new ProgramDispatchStaleError(\n            "ProgramAttempt became active after the protected routing observation cut",\n          );\n        }\n\n        const programStateId = String(asProgramStateId(program.programStateId));\n        const programAttemptId = String(asProgramAttemptId(program.programAttemptId));\n        const workItemId = String(asProgramWorkItemId(program.workItemId));\n        if (!Number.isSafeInteger(program.agentGeneration) || program.agentGeneration <= 0) {\n          throw new ProgramDispatchControlError("agentGeneration must be a positive safe integer");\n        }\n        const state = requireProgramState(events, programStateId);\n        requireExactRevision(state, program.expectedProgramRevision);\n        if (!sessionIsActive(events, sessionId) ||\n            !state.attachedSessionIds.some((id) => String(id) === sessionId)) {\n          throw new ProgramDispatchStaleError("ProgramAttempt session is stopped or detached");\n        }\n        const attempt = state.activeAttempt;\n        if (attempt === null || String(attempt.programAttemptId) !== programAttemptId) {\n          throw new ProgramDispatchStaleError("ProgramAttempt authority is stale");\n        }\n        if (String(attempt.workItemId) !== workItemId || String(attempt.sessionId) !== sessionId) {\n          throw new ProgramDispatchStaleError("ProgramAttempt work/session authority is stale");\n        }\n        if (attempt.agentGeneration !== program.agentGeneration ||\n            !await this.options.agentGenerations.isCurrent(sessionId, program.agentGeneration)) {\n          throw new ProgramDispatchStaleError("ProgramAttempt Agent generation is stale");\n        }\n        if (!await this.options.recovery.isClear()) {\n          throw new ProgramDispatchStaleError("Program recovery barrier is not clear");\n        }\n        const writers = outstandingWriterOperations(events);\n        if (writers.length > 0) {\n          throw new ProgramDispatchStaleError(`Outstanding Workspace writer barrier: ${writers.join(",")}`);\n        }\n        if (state.executionBaseMismatch !== null || state.executionBaseUnavailable) {\n          throw new ProgramDispatchStaleError("Program execution base is not current");\n        }\n        if (!sameBase(attempt.expectedExecutionBase, protectedObservation)) {\n          throw new ProgramDispatchStaleError(\n            "ProgramAttempt execution base no longer matches the protected current base",\n          );\n        }\n\n        const ownership = {\n          programStateId,\n          expectedProgramRevision: program.expectedProgramRevision,\n          programAttemptId,\n          workItemId,\n          agentGeneration: program.agentGeneration,\n        };\n        const stamped = input.drafts.map((draft, index) => {\n          if (String(draft.workspaceId) !== this.options.store.workspaceId) {\n            throw new ProgramDispatchControlError("Program operation draft belongs to another Workspace");\n          }\n          if (String(draft.sessionId) !== sessionId) {\n            throw new ProgramDispatchControlError("Program operation draft session does not match Attempt authority");\n          }\n          if (draft.operationId === undefined || String(draft.operationId) !== operationId) {\n            throw new ProgramDispatchControlError("Program operation draft operationId does not match root operation");\n          }\n          if (draft.programStateId !== undefined && String(draft.programStateId) !== programStateId) {\n            throw new ProgramDispatchControlError("Program operation draft ProgramStateId does not match Attempt authority");\n          }\n          return {\n            ...draft,\n            ...(index === 0 ? { payload: { ...record(draft.payload), ...ownership } } : {}),\n            programStateId: asEventProgramStateId(programStateId),\n          };\n        });\n        return {\n          status: "appended",\n          events: await this.options.store.append(stamped),\n          program,\n        } as const;\n      });\n    });\n  }\n\n'''
text = replace_once(text, marker, method + marker, "atomic routed root operation method")
path.write_text(text)

# ---------------------------------------------------------------------------
# Capability broker: defer Program routing until the canonical root append.
# ---------------------------------------------------------------------------
path = Path("packages/host-runtime/src/capability-broker.ts")
text = path.read_text()
text = replace_once(
    text,
    '''    const capability = registration.capability;\n    const frozenArgs = freezeCanonical(request.args);\n    const workspaceAccessClass = workspaceAccessClassOf(capability);\n    const isReadOnly = workspaceAccessClass !== "may_write";\n    const currentProgram = await resolveCurrentProgramOperationContext(this.store, request.sessionId);\n    const program = request.program ?? currentProgram;\n    if (program !== undefined && program !== null && this.programOperationAuthority === undefined) {\n      return this.finish(request, {\n        outcome: "denied",\n        errorCode: "program_operation_authority_unavailable",\n        error: "An active ProgramAttempt requires protected Program operation authority",\n      });\n    }\n    const approvalKey = this.approvalKey(request.sessionId, request.toolName);\n''',
    '''    const capability = registration.capability;\n    const frozenArgs = freezeCanonical(request.args);\n    const workspaceAccessClass = workspaceAccessClassOf(capability);\n    const isReadOnly = workspaceAccessClass !== "may_write";\n    const approvalKey = this.approvalKey(request.sessionId, request.toolName);\n''',
    "remove pre-admission Program routing",
)
old_block = '''    const quiescenceBinding = workspaceAccessClass === "may_write" ? operationScopedQuiescence(capability) : undefined;\n    if (program !== undefined && program !== null && workspaceAccessClass === "may_write") {\n      if (quiescenceBinding === undefined) {\n        return this.finish(request, {\n          outcome: "denied",\n          errorCode: "program_quiescence_unsupported",\n          error: `Program-linked may_write capability lacks a supported operation-scoped quiescence contract: ${request.toolName}`,\n        });\n      }\n      return this.finish(request, {\n        outcome: "denied",\n        errorCode: "program_mutation_settlement_pending",\n        error: "Program-linked may_write execution remains non-admitting until confirmed-effect generation advancement and post-quiescence observation settlement are installed",\n      });\n    }\n\n'''
text = replace_once(text, old_block, '    const quiescenceBinding = workspaceAccessClass === "may_write" ? operationScopedQuiescence(capability) : undefined;\n\n', "defer Program may_write decision")
text = replace_once(
    text,
    '    const programEnvelope = program ? { programStateId: asProgramStateId(program.programStateId) } : {};\n',
    '',
    "remove pre-route Program envelope",
)
text = text.replace('eventId: mkEventId(), workspaceId, sessionId: request.sessionId, operationId, ...programEnvelope,', 'eventId: mkEventId(), workspaceId, sessionId: request.sessionId, operationId,', 3)
old_payload = '''          ...(program ? {\n            programStateId: program.programStateId,\n            expectedProgramRevision: program.expectedProgramRevision,\n            programAttemptId: program.programAttemptId,\n            workItemId: program.workItemId,\n            agentGeneration: program.agentGeneration,\n          } : {}),\n'''
text = replace_once(text, old_payload, '', "remove pre-route ownership payload")
old_pre = '''    const pre = program\n      ? await this.programOperationAuthority!.appendRootOperation(\n          { ...program, sessionId: request.sessionId, operationId: operationId as string },\n          preDrafts,\n        )\n      : await this.admission.append(preDrafts);\n\n    const actionEvent = pre[2];\n'''
new_pre = '''    let pre: PersistedDomainEvent<string, unknown>[];\n    let program: ProgramCapabilityOperationContextV1 | null = null;\n    if (this.programOperationAuthority !== undefined) {\n      const routed = await this.programOperationAuthority.appendRoutedRootOperation({\n        sessionId: request.sessionId,\n        operationId: operationId as string,\n        workspaceAccessClass,\n        ...(request.program !== undefined ? { program: request.program } : {}),\n        drafts: preDrafts,\n      });\n      if (routed.status === "program_may_write_blocked") {\n        if (quiescenceBinding === undefined) {\n          return this.finish(request, {\n            outcome: "denied",\n            errorCode: "program_quiescence_unsupported",\n            error: `Program-linked may_write capability lacks a supported operation-scoped quiescence contract: ${request.toolName}`,\n          });\n        }\n        return this.finish(request, {\n          outcome: "denied",\n          errorCode: "program_mutation_settlement_pending",\n          error: "Program-linked may_write execution remains non-admitting until confirmed-effect generation advancement and post-quiescence observation settlement are installed",\n        });\n      }\n      pre = routed.events;\n      program = routed.program;\n    } else {\n      if (request.program !== undefined) {\n        return this.finish(request, {\n          outcome: "denied",\n          errorCode: "program_operation_authority_unavailable",\n          error: "Explicit Program operation context requires protected Program operation authority",\n        });\n      }\n      const routed = await this.admission.enqueue(async () => {\n        const currentProgram = await resolveCurrentProgramOperationContext(this.store, request.sessionId);\n        if (currentProgram !== null) return { status: "program_authority_required" as const };\n        return { status: "appended" as const, events: await this.store.append(preDrafts) };\n      });\n      if (routed.status === "program_authority_required") {\n        return this.finish(request, {\n          outcome: "denied",\n          errorCode: "program_operation_authority_unavailable",\n          error: "An active ProgramAttempt requires protected Program operation authority",\n        });\n      }\n      pre = routed.events;\n    }\n    const programEnvelope = program ? { programStateId: asProgramStateId(program.programStateId) } : {};\n\n    const actionEvent = pre[2];\n'''
text = replace_once(text, old_pre, new_pre, "atomic broker root routing")
path.write_text(text)

# ---------------------------------------------------------------------------
# Public exports for the routed root-operation seam.
# ---------------------------------------------------------------------------
path = Path("packages/host-runtime/src/index.ts")
text = path.read_text()
text = replace_once(
    text,
    '''  type ProgramRootOperationContextV1,\n  type ProgramRootOperationInputV1,\n  type ProgramRootOperationAuthorityV1,\n''',
    '''  type ProgramRootOperationContextV1,\n  type ProgramRootOperationInputV1,\n  type ProgramRoutedRootOperationInputV1,\n  type ProgramRoutedRootOperationResultV1,\n  type ProgramRootOperationAuthorityV1,\n''',
    "routed root exports",
)
path.write_text(text)

# ---------------------------------------------------------------------------
# Regression: issue an Attempt while capability policy is paused. The ordinary
# request has no Program fields and must still be routed to the newly-current
# Attempt at root admission. The pre-fix ordering would have bypassed it.
# ---------------------------------------------------------------------------
path = Path("packages/host-runtime/src/program-operation-correlation.test.ts")
text = path.read_text()
needle = '''  it("fails closed when an active ProgramAttempt exists before Host Program authority is wired", async () => {\n'''
race = '''  it("atomically routes an Attempt issued after capability handling begins but before root admission", async () => {\n    const suffix = "17";\n    const dir = mkdtempSync(join(tmpdir(), "alcode-program-operation-race-"));\n    dirs.push(dir);\n    const locked = await openLockedWorkspaceStore({ databasePath: join(dir, "workspace.sqlite"), lockPath: join(dir, "workspace.lock"), workspaceId: "018f0000-0000-7000-8000-000000000517", repositoryId: "program-operation-race" });\n    const admission = new CanonicalAdmissionQueue(locked.store);\n    const sessions = new HostSessionManager(locked, admission);\n    const session = await sessions.openOrResume();\n    const initial = program(session.sessionId, suffix);\n    await admission.append([{ eventId: mkEventId(), workspaceId: asWorkspaceId(locked.store.workspaceId), sessionId: session.sessionId, programStateId: asEventProgramStateId(String(initial.programStateId)), occurredAt: new Date().toISOString(), type: "program.created", payload: { state: initial }, payloadSchemaVersion: 1, producer: { kind: "runtime", component: "program-operation-race-test" } }]);\n    const current = base(locked.store.workspaceId);\n    const dispatch = new ProgramDispatchServiceV1({ store: locked.store, admission, workspaceCoordinator: { runExclusive: (work) => work() }, observations: { observe: async () => ({ status: "complete" as const, base: current }) }, agentGenerations: { isCurrent: (_sessionId, generation) => generation === 7 }, recovery: { isClear: () => true }, firstDispatchPlanning: { recheckAcceptedPlanningBase: async () => {} } });\n    let releasePolicy!: () => void;\n    let policyEntered!: () => void;\n    const entered = new Promise<void>((resolve) => { policyEntered = resolve; });\n    const released = new Promise<void>((resolve) => { releasePolicy = resolve; });\n    let executed = 0;\n    const broker = new CapabilityBroker(locked.store, admission, new CognitionGateway(locked), { authorizeCapability: async () => { policyEntered(); await released; return { allowed: true as const }; } }, [{ name: "inspect", workspaceAccessClass: "read_only", async execute() { executed += 1; return { result: {}, outcome: "succeeded" }; } }]);\n    broker.setProgramOperationAuthority(dispatch);\n\n    const pending = broker.execute({ sessionId: session.sessionId, toolCallId: "tc-race", toolName: "inspect", args: {} });\n    await entered;\n    const issued = await dispatch.issueAttempt({ programStateId: String(initial.programStateId), expectedProgramRevision: initial.revision, workItemId: "work-17", sessionId: session.sessionId, agentGeneration: 7 });\n    if (issued.status !== "issued") throw new Error(`expected Attempt issuance, got ${issued.status}`);\n    releasePolicy();\n    const result = await pending;\n    expect(result.outcome).toBe("succeeded");\n    expect(executed).toBe(1);\n    const requested = (await replay(locked)).find((event) => event.type === "operation.requested");\n    expect(String(requested?.programStateId)).toBe(String(initial.programStateId));\n    expect(requested?.payload).toMatchObject({ programAttemptId: issued.programAttemptId, expectedProgramRevision: issued.state.revision, workItemId: "work-17", agentGeneration: 7 });\n    locked.close();\n  });\n\n'''
text = replace_once(text, needle, race + needle, "routing race regression")
path.write_text(text)
